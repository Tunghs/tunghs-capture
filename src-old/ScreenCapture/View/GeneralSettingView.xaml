<UserControl x:Class="ScreenCapture.View.GeneralSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScreenCapture.View"
             xmlns:Control="clr-namespace:ScreenCaptureControls.Controls;assembly=ScreenCaptureControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    
    <Grid>
        <Control:FilePathTextBox Grid.Row="1" FilePath="{Binding DefaultSavePath, Mode=TwoWay}"
                                 VerticalAlignment="Top"
                                 Command="{Binding ButtonClickCommand}"/>
    </Grid>
</UserControl>
